import 'package:flutter/material.dart';

class ManufacturingOrders extends StatelessWidget {
  const ManufacturingOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [TextField()],
      ),
    );
  }
}
